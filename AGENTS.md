# AGENTS.md Lapis

## Project




## 基本ルール

- 回答、コメント、ドキュメントは原則として日本語で簡潔に書く。
- 変更前に `REQUIREMENTS.md` と `ARCHITECTURE.md` を確認する。
- 依頼された範囲だけを変更し、無関係な変更を上書きしない。
- 未確定事項を推測で確定しない。判断が必要なら選択肢と影響を示す。
- 指示なくコミット、プッシュを行わない。依存追加は必要性と影響を確認する。

## 設計ルール

- UI はワークスペースのファイル、Git、LSP、タスクを直接操作しない。
- UI とバックエンドは、ローカルでも定義済みの境界を通して連携する。
- 入力、選択、スクロールなど即時性が必要な処理はバックエンド応答に依存させない。
- `Project`、`Workspace`、`Conversation`、`Task`、`Execution` を混同しない。
- モードは固定実装の分岐ではなく、能力の組み合わせとして扱う。
- Document の変更には Revision を持たせ、将来のリモート・共同編集を妨げない。
- 外部 CLI 固有の出力形式を UI へ漏らさない。

## 未確定事項

次の項目は採用が決まるまで固定しない。

- 非同期ランタイム、プロセス間通信、リモート通信
- 永続化方式、共同編集アルゴリズム、プラグイン方式

決定した内容は、理由と影響範囲を `ARCHITECTURE.md` に追記する。

## 検証

- 変更に対応する整形、静的解析、テスト、ビルドを実行する。
- コマンドが未整備または実行不能なら、未検証項目と理由を報告する。
- UI 変更は実際のアプリウィンドウを画像で確認する。
- UI 確認用コマンドが整備された後は、全画面ではなく対象ウィンドウのキャプチャを使う。
- UI を目視確認せず、UI 作業を完了としない。

## ドキュメント

- `AGENTS.md`: 開発とレビューの規約
- `ARCHITECTURE.md`: 確定した設計境界と未確定事項
- `REQUIREMENTS.md`: 製品要件、優先度、対象範囲

要件変更は `REQUIREMENTS.md`、設計決定は `ARCHITECTURE.md` へ反映する。
