# REQUIREMENTS.md

## 1. 製品概要

Lapis は、メモ、コード編集、AIタスク、リモート開発を一つの会話単位で扱う軽量なデスクトップエディターです。

Fleet の軽快な操作思想を参考にしつつ、特定製品の再実装ではなく、次を独自の中心価値とします。

- メモから本格開発まで能力を段階的に追加できる。
- 会話を切り替えるだけで作業環境全体を切り替えられる。
- 人間の編集と複数のAI CLIタスクを同じ画面で扱える。
- バックエンドをローカル、リモート、コンテナへ置き換えられる。

## 2. 対象

初期利用者は開発者本人とし、初期検証環境は Windows とします。対応OSは未確定ですが、他OS対応を妨げない設計にします。

## 3. 用語

用語の責務は `ARCHITECTURE.md` の中心モデルに従います。特に `Project`、`Workspace`、`Conversation`、`Task`、`Execution` を別の概念として扱います。

## 4. 機能要件

### 4.1 編集

- `FR-EDIT-01` コードと Markdown を同じ編集基盤で開ける。
- `FR-EDIT-02` 入力、選択、Undo／Redo、検索、複数文書の切り替えができる。
- `FR-EDIT-03` 構文表示、診断、補完、定義移動を必要時に有効化できる。
- `FR-EDIT-04` バックエンド遅延時も入力と閲覧を継続できる。

### 4.2 メモ

- `FR-NOTE-01` Markdown の閲覧と編集ができる。
- `FR-NOTE-02` Wiki Link、バックリンク、タグ、見出しを扱える。
- `FR-NOTE-03` ノートからファイルやコードシンボルへリンクできる。
- `FR-NOTE-04` ノート機能だけを使う軽量な状態で起動できる。

### 4.3 開発支援

- `FR-DEV-01` LSP を起動、停止、再接続できる。
- `FR-DEV-02` Git、全文検索、タスク、ターミナルを利用できる。
- `FR-DEV-03` 大規模開発向けの永続索引とRAMキャッシュを必要時に利用できる。
- `FR-DEV-04` 未使用の解析機能を休止し、メモリを解放できる。
- `FR-DEV-05` デバッグビルドでは、画面上のGPUI要素を選択し、生成元、実レイアウト、`StyleRefinement` を本体と独立したInspectorウィンドウで確認できる。

### 4.4 会話ワークスペース

- `FR-CONV-01` 会話ごとに Workspace と画面状態を保持できる。
- `FR-CONV-02` 会話の切り替えで、文書、ファイルツリー、差分、診断、ターミナルを復元できる。
- `FR-CONV-03` 会話は共有ディレクトリ、Git worktree、リモート、コンテナへ紐づけられる。
- `FR-CONV-04` 一つの会話に複数の Task と Execution を持てる。

### 4.5 AIタスク

- `FR-AI-01` Codex などの外部 CLI をバックエンドから起動できる。
- `FR-AI-02` 複数タスクを並列実行し、状態とログを一覧できる。
- `FR-AI-03` 入力待ち、承認待ち、成功、失敗、取消を区別できる。
- `FR-AI-04` CLIごとの差異を共通表示へ変換できる。
- `FR-AI-05` 書き込み競合を検知し、必要に応じて worktree で分離できる。
- `FR-AI-06` UIを閉じても、許可された実行をバックエンドで継続できる。

### 4.6 リモートと共同作業

- `FR-REMOTE-01` リモート環境のファイル、LSP、Git、ターミナル、CLIを利用できる。
- `FR-REMOTE-02` 切断後に再接続し、編集状態と実行状態を復元できる。
- `FR-PAIR-01` 複数利用者で文書、カーソル、選択、Presenceを共有できる。
- `FR-PAIR-02` Owner、Editor、Reviewer、Viewer の権限を持てる。

### 4.7 機能拡張とカスタマイズ

- `FR-EXT-01` 外部拡張をすべて無効にしても、プレーンテキストの表示、編集、保存、Workspace と Conversation の復元ができる。
- `FR-EXT-02` 標準機能は同梱の有無と常駐の有無を分け、表示またはコマンド実行などの activation 条件を満たすまで重い処理を開始しない。
- `FR-EXT-03` 言語定義、構文、LSP、formatter、テーマ、アイコン、表示言語、キーマップを交換または追加できる境界を持つ。
- `FR-EXT-04` コマンド、アイコン、翻訳文言は表示文字列ではなく、翻訳されない安定した ID で参照する。
- `FR-EXT-05` 拡張 UI は Tool Dock、Side Dock、Bottom Dock、Status Bar、Editor Decoration など、Lapis が管理する定義済み slot へ追加する。
- `FR-EXT-06` 拡張が利用する Workspace、process、network などの能力を宣言し、許可されていない外部環境を直接操作させない。
- `FR-EXT-07` 右ペインの汎用検索ページから安定したCommand IDを検索、選択、実行でき、検索対象をProviderとして追加できる。

## 5. 非機能要件

- `NFR-PERF-01` Note 状態では不要な LSP、索引、AI CLI を起動しない。
- `NFR-PERF-02` 入力とスクロールを通信や重い解析から分離する。
- `NFR-PERF-03` 機能の installed、loaded、running を別状態として扱い、未使用の同梱機能を常駐させない。
- `NFR-RELI-01` Document の Revision を検証し、更新消失を防ぐ。
- `NFR-RELI-02` 異常終了後に会話、文書、タスク状態を復元できる。
- `NFR-SEC-01` 秘密情報をログ、会話、リポジトリへ平文保存しない。
- `NFR-SEC-02` AI CLI の実行場所、権限、対象 Workspace を明示する。
- `NFR-TEST-01` UIを対象ウィンドウ単位で撮影し、目視検証できる。
- `NFR-MAINT-01` UI、中心モデル、外部ツール連携を交換可能な境界で分離する。
- `NFR-MAINT-02` 標準機能も可能な範囲で外部拡張と同じ command、contribution、activation の契約を利用する。
- `NFR-TEST-02` 汎用検索ページはタイトルバーの検索ボタン、`Ctrl/Cmd Shift K`、500ms以内のShift二度押しから開け、Shift長押しや途中の通常キー入力では誤起動しない。

具体的な起動時間、メモリ量、入力遅延の目標値は、計測基盤を作成後に決定します。

## 6. 開発フェーズ

| フェーズ | 範囲 | 優先度 |
| --- | --- | --- |
| 0 | リポジトリ調査、中心モデル、境界、状態遷移、依存方向の整合 | 必須 |
| 1 | ローカルWorkspace、ファイルツリー、複数文書、実用編集、安全な保存と復元 | 必須 |
| 2 | Codex CLI、Task / Execution、共通イベント、並列実行、入力・承認、履歴復元 | 必須 |
| 3 | Git status / diff、Task worktree、レビュー、選択取込・破棄、競合表示 | 必須 |
| 4 | rust-analyzer候補のLSP、全文検索、PTY Terminal、Problems / Output | 必須 |
| 5 | Conversation全状態復元、backend交換、切断・再接続、remoteまたはloopback検証 | 必須 |
| 次段階 | UI module分割、標準Feature registry、遅延activation、theme・locale・language定義のデータ駆動化 | 高 |
| 将来 | ノートリンク、コードリンク、永続索引、高度解析、共同編集、配布基盤 | 対象外 |

詳細なモデル、状態遷移、crate境界、各フェーズの完了条件は `ARCHITECTURE.md` に従います。

### 6.1 段階 1 の UI 受け入れ条件

- Fleet 最終版の配置を参考に、上部バー、左ツール島、中央エディター島を分離して表示する。
- 上部バーはアプリとレイアウトの操作、Workspace とブランチ、実行系の操作を左右と中央に分ける。
- Windows では独立した OS 標準タイトルバーを表示せず、移動、最小化、最大化、閉じる操作を上部バーへ統合する。
- 左ツール島の `Files`、`Search`、`Git`、`History` は上部の横タブで切り替える。
- 文書タブ、パンくず、編集領域、文書固有のステータスを中央エディター島へまとめる。
- 下部パネル、Markdown プレビュー、Assistant は必要時だけ表示し、初期状態では編集領域を優先する。
- 新規、開く、保存、パネル切り替えはキーボードとコマンド導線から実行できる。
- パネルの選択、hover、focus、未保存、成功、失敗を視覚的に区別できる。
- 実装済みバックエンドがない機能は結果を偽装せず、空状態または利用不可として表示する。
- 1440 x 900 を基準に対象ウィンドウを撮影し、Fleet 参考画面と比較して目視確認する。

## 7. 対象外

初期段階では次を対象外とします。

- IntelliJ 相当の全言語解析とリファクタリング
- 複数OSへの同時提供
- 第三者向けプラグイン配布基盤
- 第三者の任意GPUIコードによる無制限なUI差し替え
- クラウドサービスの運営
- 完全な共同編集

## 8. 未確定事項

- 複数文書、Undo／Redo、構文表示に対応する編集バッファの発展方式
- Protocol、IPC、リモート通信の方式
- 永続化、索引、共同編集の方式
- 最初に対応する LSP と AI CLI
- 定量的な性能目標
- 製品名と配布方式の最終決定
- 外部拡張のpackage形式、実行sandbox、署名、配布store
