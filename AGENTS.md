# AGENTS.md

Lapis 全体に適用する開発・レビュー規約です。下位の `AGENTS.md` は、その領域にだけ追加の制約を与えます。

## 作業前

- 回答、コメント、ドキュメントは原則として日本語で簡潔に書く。
- `ARCHITECTURE.md` と、変更対象に最も近い `AGENTS.md` を確認する。
- 見た目や UI の統一に関する変更では、存在する場合は `DESIGN.md` も確認する。
- 依頼された範囲だけを変更し、利用者の未関連な変更を維持する。
- 未確定事項を推測で固定しない。依存追加や不可逆な判断は、必要性と影響を明らかにする。
- 指示なくコミット、プッシュ、外部公開を行わない。

## 責務による構成

モジュールは行数ではなく、責務と境界で分ける。判断には高凝集・低結合、単一責任、情報隠蔽を用いる。

- 一つのモジュールには、一つの主要な変更理由と明確な状態所有者を置く。
- 状態、ライフサイクル、不変条件、外部 I/O の境界が異なる場合は分離する。
- 同じ変更で一緒に変わり、同じ不変条件を守る処理は近くに置く。
- 分離後は、公開 API を必要最小限にし、内部状態を境界の外から直接変更させない。
- 外部システムやフレームワーク固有の型は adapter 内に閉じ、Lapis の契約へ変換する。
- 依存は安定した契約へ向け、具体実装の組み立てはアプリの entry point に限定する。

ファイルが大きいことは設計を見直す合図だが、それ自体は分割理由ではない。単に別ファイルへ移す、薄い転送層を増やす、共有 mutable state を複数モジュールから触る、といった分割は避ける。`common`、`utils`、`manager`、`service` を責務不明な置き場にしない。

新しい境界を作るときは、少なくとも次を説明できること。

- 何を所有し、何を所有しないか。
- どの変更理由を閉じ込めるか。
- 入出力となる契約は何か。
- 単独で検証できる振る舞いは何か。

将来用の空モジュールや未実装機能は作らない。着手していない機能は実装済みの経路から分離し、存在するように見せない。

## アーキテクチャ境界

- UI はファイル、Git、LSP、Terminal、Task、永続化を直接操作しない。
- UI とバックエンドは、同一プロセスでも定義済みの command、query、event の境界を通す。
- 入力、選択、スクロールなど即時性が必要な UI 操作は、バックエンド応答を待たせない。
- `Project`、`Workspace`、`Conversation`、`Task`、`Execution` を混同しない。
- Document の変更には Revision を持たせ、古い非同期応答を適用しない。
- 外部 CLI、SDK、通信ライブラリ固有の形式を UI や core へ漏らさない。

## 検証

- 変更に対応する整形、静的解析、テスト、ビルドを実行する。
- 実行できない検証は、項目と理由を報告する。
- UI 変更は対象アプリを実行し、対象ウィンドウを画像で目視確認する。
- リファクタリングでは、移動後も責務と依存方向が改善されていることを確認する。

### Windows の GPUI 画面検証

- 「ディスプレイ消灯」と「システムスリープ」を分けて扱う。S3 スリープや休止状態では CPU、GPU、GPUI のイベントループが動かないため、その状態のまま新しい画面を撮影することはできない。
- 無人の起動・撮影では、撮影区間だけ `SetThreadExecutionState(ES_CONTINUOUS | ES_SYSTEM_REQUIRED | ES_DISPLAY_REQUIRED)` または同等の `PowerSetRequest` を設定し、完了直後に解除する。これはアイドルによる消灯・スリープを防ぐもので、電源ボタンや蓋を閉じる操作によるスリープは防がない。`ES_AWAYMODE_REQUIRED` は通常の UI 検証では使わない。
- Windows の GPUI は Direct3D/DirectComposition を使うため、`PrintWindow` や GDI の `BitBlt` だけを画面検証の根拠にしない。黒いクライアント領域は描画不良ではなく、GPU サーフェスを取得できていない結果の場合がある。
- キャプチャは、対話中のユーザーセッションと実 GPU がある環境で `Windows.Graphics.Capture` または Desktop Duplication を優先し、取得画像を目視確認する。`PrintWindow` は非 GPU ウィンドウの補助診断に限る。
- 実行前後に、対象プロセスのウィンドウハンドル・応答状態と電源要求を確認する。対話デスクトップや GPU が利用できない場合は、画面検証を未実施として理由を報告し、利用者のスクリーンショットを代替証跡にする。

## ドキュメントの責務

- `AGENTS.md`: 実装・変更・レビュー時に守る規約。
- `ARCHITECTURE.md`: コードだけでは分からない設計理由、不変条件、依存境界。
- `DESIGN.md`: 見た目、視覚的一貫性、UI コンポーネントの表現規則。
- 製品要件、優先度、作業範囲: issue や依頼文など、作業を管理する場所。

ディレクトリ一覧、型定義、実装状況など、ソースから直接分かる情報を文書へ重複させない。文書には「なぜその境界が必要か」と「何を壊してはいけないか」だけを残す。
